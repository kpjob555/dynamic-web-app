import styled from 'styled-components'

export const ScSidebar = styled.aside`
  width: 260px;
  padding: 20px;
  background: linear-gradient(180deg, rgba(30, 27, 75, 0.6) 0%, rgba(49, 46, 129, 0.4) 100%);
  border-right: 1px solid rgba(139, 92, 246, 0.2);
  backdrop-filter: blur(10px);
`
